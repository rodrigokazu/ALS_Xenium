# archive

History, kept so the reasoning behind the current pipeline stays readable. Nothing here
feeds `ck_spatial_pipeline/`.

| Folder | What it was |
|---|---|
| `pipeline_gausss/` | The generation on Marcel's Gaussian re-segmentation, including the D1014/D1018 sub-domain chain on the June joint Novae run. |
| `hybrid_segmentation/` | Xenium Ranger polygon and TIFF exports from the hybrid segmentation attempt. |
| `novae_v1/` | The first per-sample Novae run, one job looping over all samples. |
| `qc_clustering/` | The standalone scanpy QC, clustering and embedding sweep. |
| `cellannotator_prototype/` | The July CellAnnotator test on SD03522_BG with claude-haiku-4-5. The later v6 and v7 branch on SCG never reached the canonical object. |
| `notebooks_legacy/` | The pre-dual-pass QC and concatenation notebooks. |
